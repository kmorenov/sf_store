twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

    globals:
        cart_manager_service: '@App\Service\CartManager'

        product_images_service: '@App\Service\ProductImages'
