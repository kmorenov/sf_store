{% form_theme imageForm 'bootstrap_4_layout.html.twig' %}

{{ form_start(imageForm) }}
    {{ form_row(imageForm.filepath, {
        'label_attr': {'class': 'btn btn-info', 'style': 'cursor: pointer;'}, 'attr': {'style': 'display: none;'}}
    ) }}
    {{ form_row(imageForm.product) }}
    {#{{ form_widget(imageForm) }}#}
{{ form_end(imageForm) }}