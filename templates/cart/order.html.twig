{% extends 'base.html.twig' %}
{#{% use 'Symfony\Component\HttpFoundation\Request' %}#}
{#{% set request = {{ Request::createFromGlobals() }} %}#}

{% block body %}
    <h1>Order</h1>
    <form action="cart/order_confirm.html.twig">
        {#<input type="hidden" name="productid" value="{{ product[0].id }}">#}
        {#{{ product[0].model ~ ' ' ~ product[0].price }}#}


        {% dump cart %}

        {% set i = 0 %}
        {% for p in cart %}
            {#{% if p.quantity is defined %}#}
            Product ID: {{ p.id }}
            Price: {{ p.price }}
            Quantity: {{ p.quantity }} <br/>{# [i][p.id] }}#}
            {#{% set i = i+1 %}#}
            {#{% endif %}#}

        {% endfor %}
        <button type="submit">Order</button>
    </form>
{% endblock %}